# tcp_connect_proxy
 A simple HTTP CONNECT proxy written in Go. Accepts CONNECT requests from clients like curl, establishes a TCP tunnel to the destination server, and forwards raw bytes bidirectionally.
